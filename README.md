# RAG-PROJECT

install uv globally in system - brew install uv / curl -LsSf https://astral.sh/uv/install.sh | sh

create virtual environment - python3 -m venv .venv

run virtual environment - source .venv/bin/activate

deactivate virtual environment - deactivate

install requirements from requirements.txt file - uv pip install -r requirements.txt
